import pytest
import dhimmis

# TODO start in a "PYTEST" environment
# In this environment, connect to a database `pytest`
# Add a fixture `database` that rebuilds that database prior to each test
# Maybe this can happen in RAM?
