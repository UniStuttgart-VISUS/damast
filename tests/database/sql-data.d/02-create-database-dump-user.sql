CREATE USER ro_dump LOGIN PASSWORD 'dump';
GRANT CONNECT ON DATABASE ocn TO ro_dump;

ALTER DEFAULT PRIVILEGES
  GRANT SELECT
  ON TABLES TO ro_dump;
ALTER DEFAULT PRIVILEGES
  GRANT USAGE, SELECT
  ON SEQUENCES TO ro_dump;
