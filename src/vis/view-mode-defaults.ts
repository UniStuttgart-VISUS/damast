import { DisplayMode, TimelineMode, MapMode, SourceViewSortMode } from './datatypes';

export const show_only_active: boolean = true;
export const display_mode: DisplayMode = DisplayMode.Religion;
export const timeline_mode: TimelineMode = TimelineMode.Qualitative;
export const map_mode: MapMode = MapMode.Clustered;
export const source_sort_mode: SourceViewSortMode = SourceViewSortMode.ByCountDescending;
